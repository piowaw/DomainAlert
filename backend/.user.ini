; PHP-FPM timeout settings for DomainAlert
; Plesk uses PHP-FPM which reads .user.ini instead of .htaccess php_value directives
max_execution_time = 600
max_input_time = 600
default_socket_timeout = 600
memory_limit = 256M
